<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:colorBackground">@color/colorCustom</item>
        <item name="colorSurface">@color/colorCustom</item>
        <item name="buttonBarButtonStyle">@style/Button</item>
        <item name="android:statusBarColor">?colorSurface</item>
        <item name="dialogCornerRadius">8dp</item>
    </style>

    <style name="SplashTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Button" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:layout_margin">4dp</item>
        <item name="textAllCaps">false</item>
    </style>

<!--
    MAL doesn't use Theme.MaterialComponents.DayNight yet
-->
    <style name="AppTheme.AboutCardLight" parent="Theme.Mal.Light">
        <item name="mal_card_background">@color/aboutCardBackground</item>
    </style>

    <style name="AppTheme.AboutCardDark" parent="Theme.Mal.Dark">
        <item name="mal_card_background">@color/aboutCardBackground</item>
    </style>
    
    <style name="PreviewCards" parent="Widget.MaterialComponents.CardView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="cardCornerRadius">8dp</item>
        <item name="cardUseCompatPadding">true</item>
    </style>

    <style name="PreviewText" parent="Widget.MaterialComponents.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingTop">32dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
    </style>
</resources>
